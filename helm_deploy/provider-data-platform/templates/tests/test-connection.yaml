apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "provider-data-platform.fullname" . }}-test-connection"
  labels:
    {{- include "provider-data-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "provider-data-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
